<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ContratIA — Genera Contratos para Autónomos con IA | Ley Española</title>
    <meta name="description" content="Genera contratos profesionales en segundos con inteligencia artificial. Adaptados a la ley española.">
    <style>
        :root { --ink: #0a0a0f; --cream: #f5f0e8; --gold: #c9a84c; --paper: #faf7f2; }
        body { background: var(--paper); color: var(--ink); font-family: 'Inter', system-ui, sans-serif; margin: 0; padding: 0; }
        nav { padding: 1.5rem; background: rgba(250,247,242,0.85); border-bottom: 1px solid rgba(201,168,76,0.1); text-align: center; }
        .logo { font-size: 1.5rem; font-weight: 800; letter-spacing: -0.02em; }
        .logo span { color: var(--gold); }
        .hero { padding: 4rem 1.5rem; text-align: center; max-width: 800px; margin: 0 auto; }
        h1 { font-size: 2.5rem; line-height: 1.1; margin-bottom: 1rem; color: var(--ink); }
        p { color: #4a4a4f; font-size: 1.1rem; margin-bottom: 2rem; }
        .btn { background: var(--ink); color: var(--cream); padding: 1rem 2rem; text-decoration: none; border-radius: 6px; font-weight: 600; display: inline-block; }
        .preview-box { margin-top: 3rem; background: white; border: 1px solid #e0ddd7; border-radius: 12px; padding: 2rem; box-shadow: 0 10px 30px rgba(0,0,0,0.05); }
    </style>
</head>
<body>
    <nav>
        <div class="logo">Contrat<span>IA</span></div>
    </nav>
    <section class="hero">
        <h1>Contratos <em>perfectos</em> en segundos.</h1>
        <p>El primer asistente legal inteligente diseñado específicamente para autónomos y profesionales en España.</p>
        <a href="#" class="btn">CREAR MI CONTRATO</a>
        <div class="preview-box">
            <small style="color: var(--gold); text-transform: uppercase; letter-spacing: 0.1em; font-weight: 700;">Ejemplo de uso</small>
            <p style="font-style: italic; margin-top: 1rem;">"Redacta un contrato de prestación de servicios para una diseñadora gráfica que incluye una cláusula de confidencialidad..."</p>
        </div>
    </section>
</body>
</html>
